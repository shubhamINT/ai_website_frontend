
# Real-time agent (LiveKit) — both used client-side and server-side (no NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_LIVEKIT_URL=""
# Backend API — both used client-side and server-side (no NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_BACKEND_URL=""
# Pixabay API key — used client-side only (NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_PIXABAY_API_KEY=""

# your app's public base URL (origin)
APP_URL="http://localhost:3000"

